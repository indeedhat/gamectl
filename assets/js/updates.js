// Updates css colors if a server status changes, should be called from main.js each time servers 
// are pinged for status

//Remove Me Later Dumbass.
update_css();
function update_css(){
    var status = document.getElementsByClassName("status");
    console.log(status);
}